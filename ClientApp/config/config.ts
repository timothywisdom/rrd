/**
 * Main Configuration File for Front End App
 */

const config = {
	// List of supported languages. Each one must have a matching /ClientApp/assets/i18n/{language}.json file
	supported_languages: ["en", "fr", "de", "es", "pt", "nl", "it", "da", "cs", "sv", "pl", "fi", "no"],
};

export default config;
